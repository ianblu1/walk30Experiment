-provide(:title, 'Active Participants')
%h1 All Participants


=will_paginate

%table{:cellspacing => "5"}
	%thead
		%th ID
		%th Date
		%th PST Time
		%th Messages Sent
		%th Messages Received
		%th Flags
	%tbody
		=render :partial => "participant_admin", :collection => @participants, :as => :participant


