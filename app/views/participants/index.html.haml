-provide(:title, 'All Participants')
%h1 All Participants

=will_paginate


%table{:cellspacing => "5"}
    %thead
        %th ID
        %th Last Sent Message Time 
        %th Last Received Message Time 
        %th Last Received Message Content
		
    %tbody
        =render :partial => "participant_admin", :collection => @participants, :as => :participant


